name: spec

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ 'master' ]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        # ldoc needs a new release to support 5.5:
        # https://github.com/lunarmodules/ldoc/issues/426 .
        #
        # lua-version: ["5.5", "5.4", "5.3", "5.2", "5.1", "luajit"]
        lua-version: ["5.4", "5.3", "5.2", "5.1", "luajit"]
        strict: ["std.strict", ""]

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: luarocks/gh-actions-lua@v12
        with:
          luaVersion: ${{ matrix.lua-version }}

      - uses: luarocks/gh-actions-luarocks@v7

      - name: install
        run: |
          sudo apt-get install -y libyaml-dev
          luarocks install ansicolors
          luarocks install ldoc
          luarocks install luacov
          luarocks install specl
          luarocks install std._debug
          luarocks install typecheck
          test -n "${{ matrix.strict }}" && luarocks install std.strict || true

      - name: build
        run: |
          luarocks make --force

      - name: test
        run: |
          specl -vfreport --coverage spec/*_spec.yaml
          bash <(curl -s https://codecov.io/bash) -f luacov.report.out
